import { Condition } from '../../wait';
import { Assertable } from '../../entity';
export declare namespace should {
    const match: <T extends Assertable>(condition: Condition<T>) => (entity: T) => Promise<T>;
}
