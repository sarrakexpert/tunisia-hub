import { By } from 'selenium-webdriver';
import { Browser } from '../../browser';
import { Collection } from '../../collection';
import { ElementCondition } from '../../conditions';
import { Element } from '../../element';
export declare namespace find {
    const element: (cssOrXpathOrBy: string | By) => (context: Element | Browser) => Element;
    const collected: (searchFunction: (element: Element) => Element | Collection) => (collection: Collection) => Collection;
    const parent: (element: Element) => Element;
    const followingSibling: (element: Element) => Element;
    const elementAt: (index: number) => (collection: Collection) => Element;
    const first: (collection: Collection) => Element;
    const sliced: (start: number, end: number) => (collection: Collection) => Collection;
    const elementBy: (condition: ElementCondition) => (collection: Collection) => Element;
    const filteredBy: (condition: ElementCondition) => (collection: Collection) => Collection;
    const alert: (browser: Browser) => import("selenium-webdriver").AlertPromise;
}
