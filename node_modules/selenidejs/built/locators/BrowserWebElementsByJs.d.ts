import { WebElement } from 'selenium-webdriver';
import { Locator } from './locator';
import { Browser } from '../browser';
export declare class BrowserWebElementsByJs implements Locator<Promise<WebElement[]>> {
    private readonly browser;
    private readonly script;
    private readonly args;
    constructor(browser: Browser, script: (string | ((document: Document) => HTMLCollectionOf<HTMLElement>)), args: any[]);
    find(): Promise<WebElement[]>;
    toString(): string;
}
