import { WebElement } from 'selenium-webdriver';
import { Collection } from '../collection';
import { Element } from '../element';
import { Locator } from './locator';
export declare class CollectedByLocator implements Locator<Promise<WebElement[]>> {
    private readonly searchFunction;
    private readonly collection;
    constructor(searchFunction: (element: Element) => Element | Collection, collection: Collection);
    find(): Promise<WebElement[]>;
    toString(): string;
}
