import { WebElement } from 'selenium-webdriver';
import { Element } from '../element';
import { Locator } from './locator';
export declare class ElementWebElementsByJs implements Locator<Promise<WebElement[]>> {
    private readonly element;
    private readonly script;
    private readonly args;
    constructor(element: Element, script: (string | ((element: HTMLElement) => HTMLCollectionOf<HTMLElement>)), args: any[]);
    find(): Promise<WebElement[]>;
    toString(): string;
}
