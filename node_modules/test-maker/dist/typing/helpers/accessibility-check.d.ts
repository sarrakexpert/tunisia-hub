import { ElementContext, RunOnly } from "axe-core";
interface AxeOptions {
    runOnly?: RunOnly;
    rules?: Object;
    iframes?: Boolean;
    elementRef?: Boolean;
    selectors?: Boolean;
}
export declare function axeCheck(context?: ElementContext, options?: AxeOptions): Promise<void>;
export {};
