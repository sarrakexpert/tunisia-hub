/// <reference types="node" />
import { JsonPdf } from "./json-pdf";
export declare class PDFParser {
    private pdfParser;
    constructor();
    parseFile(filePath: string): Promise<JsonPdf>;
    parseBuffer(buffer: Buffer): Promise<JsonPdf>;
    parse(buffer: Buffer): Promise<unknown>;
    extractText(pdfData: any): {
        page: number;
        rows: string[];
    }[];
    private extractPlainText;
    private normalizePDFResult;
}
export declare const pdfParser: PDFParser;
