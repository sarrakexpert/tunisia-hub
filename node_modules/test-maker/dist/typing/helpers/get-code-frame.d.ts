import { ErrorSourceCode } from "../types/error/error-source-code";
export declare function getCodeFrame({ code, lineNumber, columnNumber, linesAbove, linesBelow, arrowsColor, }: {
    code: string;
    lineNumber: number;
    columnNumber: number;
    linesAbove?: number;
    linesBelow?: number;
    arrowsColor?: string;
}): Promise<ErrorSourceCode>;
