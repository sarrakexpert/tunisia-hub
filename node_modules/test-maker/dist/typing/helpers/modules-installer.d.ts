import { InstallList } from "../test-api/installList";
export declare class ModulesInstaller {
    private isPNPM;
    constructor();
    install({ installs, save, restart }: {
        installs: InstallList;
        save?: boolean;
        restart: boolean;
    }): Promise<void>;
    private performInstall;
    private shouldInstall;
}
export declare const modulesInstaller: ModulesInstaller;
