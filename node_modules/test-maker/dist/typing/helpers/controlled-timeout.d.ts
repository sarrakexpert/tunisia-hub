import { KeywordTimeoutError } from "../error-parser/keyword-timeout-error";
export declare class ControlledTimeout {
    private readonly timeout;
    private timeoutID;
    private readonly stack;
    constructor(span: number, stack: KeywordTimeoutError);
    get start(): Promise<void>;
    clear(): void;
}
