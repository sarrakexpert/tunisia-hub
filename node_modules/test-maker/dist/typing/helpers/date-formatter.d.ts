import { I18nSettings } from 'fecha';
export declare class DateFormatter {
    i18n: I18nSettings;
    format({ date, format, utc, i18n }: {
        date: Date;
        format: string;
        utc?: boolean;
        i18n?: I18nSettings;
    }): string;
    parse(dateStr: string, format: string, i18n?: I18nSettings): Date | null;
    addMask(name: string, format: string): void;
}
export declare const dateFormatter: DateFormatter;
