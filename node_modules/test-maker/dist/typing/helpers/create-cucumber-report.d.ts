import { Engine } from '../types/engine';
import { CucumberReportFeatureSchema } from '../types/reporting/cucumber/cucumber-report-feature-schema';
export declare class CreateCucumberReport {
    private errors;
    private embeddings;
    private options;
    private ansi2Html;
    transform({ engine, useNanoSeconds, includeLocal, allowHtml, allowScreenShots, }: {
        engine: Engine;
        useNanoSeconds?: boolean;
        includeLocal?: boolean;
        allowHtml?: boolean;
        allowScreenShots?: boolean;
    }): Promise<CucumberReportFeatureSchema[]>;
    private transformFeature;
    private transformScenario;
    private transformStep;
    private transformArguments;
    private transformResult;
    private processRuns;
    private formatError;
    private embedScreenShot;
    private normalizeStatusText;
}
export declare const createReport: CreateCucumberReport;
