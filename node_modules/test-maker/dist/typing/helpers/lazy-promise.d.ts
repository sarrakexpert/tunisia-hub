export declare type LazyPromise<T> = {
    resolve: (value?: T | PromiseLike<T>) => void;
    reject: (reason?: any) => void;
} & Promise<T>;
export declare function lazyPromise<T>(): LazyPromise<T>;
