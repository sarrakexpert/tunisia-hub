import { address, animal, commerce, company, database, datatype, date, finance, git, hacker, helpers, image, internet, lorem, music, name, phone, random, system, time } from 'faker';
export interface DataFakerOptions {
    local: string;
    seed: number;
}
export declare class DataFaker {
    private faker;
    private local;
    constructor(options?: DataFakerOptions);
    seed(value: number): void;
    address(): typeof address;
    animal(): typeof animal;
    commerce(): typeof commerce;
    company(): typeof company;
    database(): typeof database;
    datatype(): typeof datatype;
    date(): typeof date;
    finance(): typeof finance;
    git(): typeof git;
    hacker(): typeof hacker;
    helpers(): typeof helpers;
    image(): typeof image;
    internet(): typeof internet;
    lorem(): typeof lorem;
    name(): typeof name;
    music(): typeof music;
    phone(): typeof phone;
    random(): typeof random;
    system(): typeof system;
    time(): typeof time;
}
export declare const fake: (options?: DataFakerOptions) => DataFaker;
