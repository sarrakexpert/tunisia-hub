import { WaitForVisualStateResult } from "../types/test-api/controller/wait-for-visual-state-reuslt";
export interface JimpDiffOptions {
    baseImagePath: string | (() => Promise<string>);
    testImagePath: string | (() => Promise<string>);
    diffImagePath?: string | (() => Promise<string>);
    mismatchThreshold?: number;
}
export interface JimpDiffResult {
    exceedThreshold: boolean;
    isSameDimensions: boolean;
    misMatchPercentage: any;
}
export declare function compareImageJimp(options: JimpDiffOptions): Promise<WaitForVisualStateResult>;
