export declare function waitUntil({ predicate, timeout, interval, message, overrideMessage }: {
    predicate: boolean | (() => boolean) | (() => Promise<boolean>);
    timeout?: number;
    interval?: number;
    message?: string;
    overrideMessage?: string;
}): Promise<unknown>;
