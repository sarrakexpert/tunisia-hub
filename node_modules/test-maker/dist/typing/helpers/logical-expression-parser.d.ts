export declare class LogicalExpressionParser {
    private readonly originalExpression;
    private tokens;
    private TokensIndexes;
    constructor(expression?: string);
    test(operands: string | string[]): boolean;
    private testBoolean;
    evaluate(booleanExpression: string): boolean;
    private replaceBooleanOperands;
    private convertToBooleanExpression;
    private replaceOperators;
}
