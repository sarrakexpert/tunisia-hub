import { LazyPromise } from '../helpers/lazy-promise';
export declare class Debugger {
    private isParallel;
    private lazyPromise;
    private inlineLazyPromise;
    private debuggingEnabled;
    private parallelMessage;
    private continueMessage;
    private infoMessage;
    init(): void;
    activate(type: 'init' | 'controller' | 'selector' | 'assertion' | 'failure'): LazyPromise<any> | undefined;
    activateInline(): LazyPromise<any>;
    deactivate(fromKeyPress?: boolean): void;
}
export declare const tmDebugger: Debugger;
