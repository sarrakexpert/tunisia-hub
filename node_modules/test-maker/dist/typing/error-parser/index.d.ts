import { CallSiteRecord } from '../types/call-site/call-site-record';
import { DetailedError } from '../types/error/detailed-error';
import { ErrorInfo } from '../types/error/error-info';
import { ErrorSourceCode } from '../types/error/error-source-code';
export declare class ErrorParser {
    parse(errorInfo: ErrorInfo): Promise<DetailedError>;
    createErrorSource({ errorInfo, callSiteRecord }: {
        errorInfo?: ErrorInfo;
        callSiteRecord: CallSiteRecord;
    }): Promise<ErrorSourceCode>;
    private getCallSite;
    private createSpecError;
}
export declare const errorParser: ErrorParser;
