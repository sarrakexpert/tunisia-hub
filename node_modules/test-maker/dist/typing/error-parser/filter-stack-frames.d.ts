import { CodeStackFrame } from "../types/call-site/code-stack-frame";
import { ErrorInfo } from "../types/error/error-info";
interface FilterStackFrameFNParams {
    frames: CodeStackFrame[];
    errorInfo?: ErrorInfo;
    extraFilterFileNames?: string[];
    filter?: boolean;
    useCustomFilter: boolean;
}
export declare type FilterStackFrameFN = (params: FilterStackFrameFNParams) => Promise<{
    targetIndex: number;
    filteredList: CodeStackFrame[];
}>;
export declare function filterStackedFrames(params: FilterStackFrameFNParams): Promise<{
    targetIndex: number;
    filteredList: CodeStackFrame[];
}>;
export {};
