import { CallSiteRecord } from "../types/call-site/call-site-record";
import { ErrorInfo } from "../types/error/error-info";
import { FilterStackFrameFN } from "./filter-stack-frames";
import { StackLineFrame } from "./parse-stack-trace";
export declare class CallSiteManager {
    createRecord({ error, errorInfo, frameIndexRetriever, extraFilterFileNames, useCustomFilter }: {
        error: Error;
        errorInfo?: ErrorInfo;
        extraFilterFileNames?: string[];
        frameIndexRetriever: number | FilterStackFrameFN;
        useCustomFilter: boolean;
    }): Promise<CallSiteRecord>;
    createCallSites(error?: Error): Promise<{
        message: string;
        originalStack: string;
        callSites: StackLineFrame[];
    }>;
    private createStackFrames;
}
export declare const callSiteManager: CallSiteManager;
