/// <reference types="node" />
import { Readable } from "stream";
import { FeatureSpec } from '../../specs';
export declare class SpecsParser {
    options: {
        includeGherkinDocument: boolean;
        includeSource: boolean;
        includePickles: boolean;
        argumentStartDelimiter: RegExp;
        argumentEndDelimiter: RegExp;
        createReadStream?: (path: string) => Readable;
    };
    private parser;
    private specs;
    private jsonMap;
    private templateParser;
    parse(paths: string[]): Promise<Map<string, FeatureSpec>>;
    private parseFeatures;
    private parseScenarios;
    private parseSteps;
    private parseTable;
    private parseTemplate;
}
export declare const specsParser: SpecsParser;
