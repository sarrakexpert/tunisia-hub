import { FeatureSpec } from '../../specs/feature-spec';
declare class SpecsAnalyzer {
    analyze(features: Map<string, FeatureSpec>): void;
    private static implementationStatus;
    private static implementationGuide;
    private featureImplementation;
    private scenarioImplementation;
    private static stepImplementation;
}
export declare const specsAnalyzer: SpecsAnalyzer;
export {};
