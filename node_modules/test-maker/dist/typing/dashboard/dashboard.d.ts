export declare class DashBoard {
    enabled: boolean;
    connected: boolean;
    private serverPort;
    private socketPort;
    private staledEvents;
    private socket;
    private resourceUsageInterval;
    init({ serverPort, socketPort }: {
        serverPort: number;
        socketPort: number;
    }): Promise<void>;
    emit(name: string, data: any): Promise<void>;
    private createServer;
    streamOutput(): Promise<void>;
    private createSocketClient;
}
