import { AdapterClientFilter } from '../adapter/adapter-client-filter';
import { Device } from '../devices';
export interface Client extends Record<any, any> {
    name: string;
    headless?: boolean;
    args?: string[];
    options?: {
        [key: string]: any;
    } & AdapterClientFilter & {
        device?: Device;
    };
}
