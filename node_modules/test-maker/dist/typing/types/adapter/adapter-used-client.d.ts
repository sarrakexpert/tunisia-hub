import { LazyPromise } from "../../helpers/lazy-promise";
import { Client } from "../client/client";
import { ClientInstance } from "./client-instance";
export interface AdapterUsedClient {
    client: Client;
    instances: Map<string, ClientInstance>;
    task: LazyPromise<any>;
    toRun: number;
    didRun: number;
}
