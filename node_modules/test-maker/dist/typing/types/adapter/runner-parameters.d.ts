import { Client } from "../client/client";
export interface RunnerParameters {
    id: string;
    client: Client;
    liveMode: boolean;
    parallel: {
        adapters: number;
        clients: number;
        features: number;
    };
}
