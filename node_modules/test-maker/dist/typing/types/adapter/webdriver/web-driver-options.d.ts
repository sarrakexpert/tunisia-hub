export interface WebDriverOptions extends Record<string, any> {
    path: string;
    baseUrl: string;
    port: number;
    waitforTimeout: number;
    connectionRetryTimeout: number;
    logLevel: string;
    maxInstances: number;
    capabilities: {
        [key: string]: any;
    };
}
