import { InstallList } from "../../test-api/installList";
import { Engine } from "../engine";
import { TestMakerPluginOptions } from "./plugin-option";
export interface TestMakerPlugin {
    name: string;
    version: string;
    description: string;
    install?: (engine: Engine, options?: TestMakerPluginOptions) => Promise<InstallList>;
    init(engine: Engine, options?: TestMakerPluginOptions): Promise<void>;
    onStart?(engine: Engine): Promise<void>;
    onRestart?(engine: Engine): Promise<void>;
    onStop?(engine: Engine): Promise<void>;
    onTaskStart?(engine: Engine): Promise<void>;
    onTaskDone?(engine: Engine): Promise<void>;
}
