import { TestPhase } from "../../gherkin/functions/testPhase";
import { ControlledTimeout } from "../../helpers/controlled-timeout";
import { FeatureSpec, ScenarioSpec, StepSpec } from "../../specs";
import { AdapterItem } from "../adapter/adapter-item";
import { Client } from "../client/client";
import { ResultRun } from "./test-run";
export interface PhaseContext {
    id: string;
    timeout: number;
    timeoutStart: Date;
    timeoutPromise?: ControlledTimeout;
    exceededTimeout: boolean;
    context: `feature` | `scenario` | `step`;
    adapterItem: AdapterItem;
    adapters: Map<string, AdapterItem>;
    clients: Map<string, Client>;
    spec: FeatureSpec | ScenarioSpec | StepSpec;
    run: ResultRun;
    isSkipped: boolean;
    isOnly: boolean;
    errorPhase?: TestPhase;
    canRunInAdapterOrClient: boolean;
    parentContext?: PhaseContext;
}
