import { SourceLocation } from "estree";
import { FeatureFn } from "../../gherkin/functions";
export interface FeatureToRunInfo {
    name: string;
    sourceCode: string;
    sourcePath: string;
    distPath: string;
    location: SourceLocation;
    adapters: string[];
    clients: string[];
    shouldRun: number;
    didRun: number;
    feature: string;
    fn?: FeatureFn;
}
