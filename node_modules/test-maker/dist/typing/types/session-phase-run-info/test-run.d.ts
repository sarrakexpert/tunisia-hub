import { TestPhase } from "../../gherkin/functions/testPhase";
import { ClientInfo } from "../client/client-info";
import { DetailedError } from "../error/detailed-error";
import { RunPhaseInfo } from "./run-phase-info";
import { TestStatus } from "./test-status";
export interface ResultRun {
    id: string;
    retries: number;
    startTime: number;
    endTime: number;
    duration: number;
    status: TestStatus;
    client: ClientInfo;
    adapter: string;
    errors: DetailedError[];
    phases: Map<TestPhase, RunPhaseInfo>;
    embeddings: {
        id: string;
        name?: string;
        mimeType: string;
        data: string;
        path: string;
        phase: TestPhase;
    }[];
}
