import { SourceLocation } from "estree";
import { GherkinKeywords } from "../../gherkin/gherkin-keywords";
import { ScenarioAstInfo } from "./scenario-ast-info";
export interface FeatureAstInfo {
    id: string;
    name: string;
    didRun: boolean;
    keyword: GherkinKeywords;
    only: boolean;
    skip: boolean;
    isLast: boolean;
    location: SourceLocation;
    scenarios: ScenarioAstInfo[];
    sourcePath: string;
    OriginalSource: string;
    distPath: string;
    adapters: string[];
    clients: string[];
}
