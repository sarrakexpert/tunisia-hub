import { GherkinAstStep } from './gherkin-ast-step';
import { GherkinAstExamples } from './gherkin-ast-examples';
export interface GherkinAstScenario {
    name: string;
    description: string;
    tags: {
        name: string;
        location: {
            line: number;
            column: number;
        };
    }[];
    keyword: string;
    location: {
        line: number;
        column: number;
    };
    steps: GherkinAstStep[];
    examples?: GherkinAstExamples[];
}
