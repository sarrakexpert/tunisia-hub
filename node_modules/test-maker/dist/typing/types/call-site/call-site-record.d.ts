import { ErrorSourceCode } from "../error/error-source-code";
import { CodeStackFrame } from "./code-stack-frame";
export interface CallSiteRecord {
    fileName: string;
    filePath: string;
    fullFilePath: string;
    message: string;
    source: string;
    lineNumber: number;
    columnNumber: number;
    frameIndex: number;
    targetFrame: CodeStackFrame;
    stack: string;
    stackFrames: CodeStackFrame[];
    filteredStackFrames: CodeStackFrame[];
    spec?: {
        sourcePath: string;
        distPath: string;
        lineNumber: number;
        columnNumber: number;
        source: ErrorSourceCode;
    };
}
