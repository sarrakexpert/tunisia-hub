import { WaitForConditionOptions } from "../../waitForConditionOptions";
export interface WaitForVisualStateOptions {
    baseImagePath: string | (() => Promise<string>);
    testImagePath: string | (() => Promise<string>);
    diffImagePath?: string | (() => Promise<string>);
    mismatchThreshold?: number;
    retryOptions?: Omit<WaitForConditionOptions, "condition" | "throwOnError">;
}
