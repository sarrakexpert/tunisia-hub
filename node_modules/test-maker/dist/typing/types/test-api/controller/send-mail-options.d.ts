import { SendMailOptions as NMSMO, SentMessageInfo as NMSMI } from 'nodemailer';
import SMTPTransport, { Options } from 'nodemailer/lib/smtp-transport';
export interface SendMailOptions {
    mailOptions: NMSMO;
    transport: SMTPTransport | Options | string;
    transportDefaults?: Options;
}
export declare type SentMessageInfo = NMSMI;
