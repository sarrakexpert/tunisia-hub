import { Request, Route } from 'playwright-core';
export interface HandleRouteParams {
    url: string | RegExp | ((url: URL) => boolean);
    handler: (route: Route, request: Request) => Promise<void>;
}
