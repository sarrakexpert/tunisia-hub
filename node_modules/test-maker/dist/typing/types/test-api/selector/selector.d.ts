import { SelectorOptions } from "./selector-options";
import { InitialSelector } from "./initial-selector";
import { SelectorPromise } from "./selector-promise";
export interface Selector extends SelectorPromise {
    (init: InitialSelector, options?: SelectorOptions): Selector;
}
