import { Socket } from 'socket.io';
export interface ClientTestMaker {
    configuration: {
        interceptNativeDialogs: boolean;
        socketPort: number;
        nativeDialogsHint: {
            alert: boolean;
            prompt: boolean;
            confirm: boolean;
            onbeforeunload: boolean;
            position: `top` | `top-start` | `top-end` | `top-left` | `top-right` | `center` | `center-start` | `center-end` | `center-left` | `center-right` | `bottom` | `bottom-start` | `bottom-end` | `bottom-left` | `bottom-right`;
            closeTimeOut: number;
        };
    };
    socketClient: Socket;
    copyPasteManger: (text: string, options: {
        format?: string;
    }) => boolean;
    clientLogMessages: {
        log: string[];
        info: string[];
        warn: string[];
        error: string[];
    };
    eventsManager: {
        on(name: string, data: {
            [key: string]: any;
        }): Promise<any>;
        emit(name: string, data: {
            [key: string]: any;
        }): Promise<any>;
        events: {
            [key: string]: (data: {
                [key: string]: any;
            }) => {};
        };
    };
    session: {
        uid: string;
        id: string;
    };
}
