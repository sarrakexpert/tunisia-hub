import { Configuration } from '../types/configuration/configuration';
import { DeepPartial } from '../types/deep-partial';
export declare class ConfigManager {
    configuration: Configuration;
    private clonedConfiguration;
    constructor();
    update(updatedConfiguration: DeepPartial<Configuration>): Promise<void>;
    restore(): Promise<void>;
    addExtraPlugins(): void;
    addExtraModules(): void;
}
export declare const configManager: ConfigManager;
export declare const updateConfiguration: (updatedConfiguration: DeepPartial<Configuration>) => Promise<void>;
