import { Engine } from "../types/engine";
export declare class FinalizeSpecs {
    private hooksTimings;
    run(engine: Engine): Promise<void>;
    private finalizeSpecs;
    private processBeforeAfterHooks;
    private processRuns;
    private processHooks;
    private finalizeSubSteps;
    private calculateSuccessPercentage;
}
export declare const finalizeSpecs: FinalizeSpecs;
