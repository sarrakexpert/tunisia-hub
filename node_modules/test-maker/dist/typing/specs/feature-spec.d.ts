import { SpecTypes } from "../types/spec-type";
import { BaseSpec } from './base-spec';
import { ScenarioSpec } from './scenario-spec';
export interface FeatureSpec extends BaseSpec {
}
declare const FeatureSpec_base: import("mix-classes/dist/types").Mixin<[typeof BaseSpec]>;
export declare class FeatureSpec extends FeatureSpec_base {
    uri: string;
    gherkinSource: string;
    type: SpecTypes;
    path: string;
    language: string;
    scenarios: Map<string, ScenarioSpec>;
}
export {};
