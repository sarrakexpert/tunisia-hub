import { TestFn } from "../../types/test-api/test-fn";
import { And, But, Given, ScenarioFn, Then, When } from "./index";
export declare class StepKeywords {
    private readonly scenario;
    constructor(scenario: ScenarioFn);
    Given(name: string, fn: TestFn): Given;
    And(name: string, fn: TestFn): And;
    When(name: string, fn: TestFn): When;
    Then(name: string, fn: TestFn): Then;
    But(name: string, fn: TestFn): But;
    protected createStep({ name, keyword, fn }: {
        name: string;
        keyword: string;
        fn: TestFn;
    }): Then | But | When | Given | And;
}
