import { Background, Example, FeatureFn, Scenario, ScenarioOutline } from "./index";
export declare class ScenarioKeywords {
    private readonly feature;
    constructor(feature: FeatureFn);
    Scenario(name: string): Scenario;
    Example(name: string): Example;
    ScenarioOutline(name: string): ScenarioOutline;
    protected Background(name: string): Background;
    protected createScenario({ name, keyword }: {
        name: string;
        keyword: string;
    }): ScenarioOutline | Example | Background | Scenario;
}
