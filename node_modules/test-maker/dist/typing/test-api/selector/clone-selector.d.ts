import { SelectorProxy } from "./selector-proxy";
export declare function cloneSelector({ selectorToClone, cloneActions, actions }: {
    selectorToClone: SelectorProxy;
    cloneActions?: boolean;
    actions?: {
        name: string;
        args: any[];
        executed: boolean;
        returnValue: boolean;
        fn: (() => any);
    }[];
}): SelectorProxy;
