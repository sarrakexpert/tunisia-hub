import { ControllerProxy } from "./controller-proxy";
export declare function controllerActionLog({ id, controllerProxy, property, propertyName, args, phase, stack, startTime, endTime, hasError }: {
    id: string;
    controllerProxy: ControllerProxy;
    property: any;
    propertyName: string;
    args: any[];
    phase: `start` | `done`;
    stack: Error;
    startTime: number;
    endTime: number;
    hasError: boolean;
}): Promise<void>;
