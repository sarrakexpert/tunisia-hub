export interface JSONServerStoreCollection {
    name: string;
}
export declare class JsonServerStore {
    private db;
    private collections;
    init(params: {
        dbFileName?: string;
    }): Promise<void>;
    createCollection(params: JSONServerStoreCollection): Promise<void>;
    create<T extends {
        id: string;
    }>({ name, data }: {
        name: string;
        data: T;
    }): Promise<{
        id: string;
    }>;
    read<T extends {
        id: string;
    }>({ id, name }: {
        id: string;
        name: string;
    }): Promise<T[] | undefined>;
    update<T extends {
        id: string;
    }>({ id, name, isPatch, data }: {
        id: string;
        name: string;
        isPatch: boolean;
        data: T;
    }): Promise<T | null>;
    delete<T extends {
        id: string;
    }>({ id, name }: {
        id: string;
        name: string;
    }): Promise<{
        id: string;
    }>;
    private checkIfCollectionExists;
}
