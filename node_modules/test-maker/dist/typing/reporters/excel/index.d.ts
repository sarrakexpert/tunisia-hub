import { Engine, TestMakerReporter } from '../..';
import { InstallList } from '../../test-api/installList';
import { TestMakerReporterOptions } from '../../types/reporting/test-maker-reporter-options';
export declare class ExcelReporter implements TestMakerReporter {
    name: string;
    private engine;
    private startDate;
    private startTime;
    private options;
    install(): Promise<InstallList>;
    init(engine: Engine, options?: TestMakerReporterOptions): Promise<void>;
    taskStart(_engine: Engine): Promise<void>;
    taskDone(engine: Engine): Promise<void>;
}
export declare const excelReporter: ExcelReporter;
