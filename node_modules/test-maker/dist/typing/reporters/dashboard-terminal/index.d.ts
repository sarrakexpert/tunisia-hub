import { FeatureSpec } from "../../specs";
import { Engine } from "../../types/engine";
import { TestMakerReporter } from "../../types/reporting/testMakerReporter";
import { RunInfo } from "../../types/session-phase-run-info/run-info";
export declare class DashboardTerminalReporter implements TestMakerReporter {
    name: string;
    private engine;
    private totalUndefined;
    private didRun;
    private runMap;
    init(engine: Engine): Promise<void>;
    taskStart(): Promise<void>;
    featureDone(feature: FeatureSpec, info: RunInfo): Promise<void>;
    taskDone(): Promise<void>;
}
export declare const dashboardTerminalReporter: DashboardTerminalReporter;
