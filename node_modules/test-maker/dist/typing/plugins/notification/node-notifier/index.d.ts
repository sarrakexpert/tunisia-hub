import { InstallList } from '../../../test-api/installList';
import { Engine } from '../../../types/engine';
import { TestMakerPlugin } from '../../../types/plugins/testMakerPlugin';
export declare class NodeNotifierPlugin implements TestMakerPlugin {
    name: string;
    version: string;
    description: string;
    private engine;
    install(): Promise<InstallList>;
    init(engine: Engine): Promise<void>;
    notify(options: {
        type: `info` | `warn` | `error`;
        title: string;
        message: string;
        sound?: boolean;
        wait?: boolean;
        time?: number;
        timeout?: number | boolean;
    }): Promise<void>;
    private registerEvents;
}
export declare const nodeNotifierPlugin: NodeNotifierPlugin;
