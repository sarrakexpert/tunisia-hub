import type { AnalyzerResult, Configuration } from 'hint';
import { Engine, TestMakerPlugin, TestMakerPluginOptions } from '../../../index';
import { InstallList } from '../../../test-api/installList';
import { DeepPartial } from '../../../types/deep-partial';
export interface WebHintPluginOptions extends DeepPartial<Configuration> {
    output: string;
}
export declare class WebHintPlugin implements TestMakerPlugin {
    name: string;
    version: string;
    description: string;
    options: WebHintPluginOptions;
    install(): Promise<InstallList>;
    init(engine: Engine, options?: TestMakerPluginOptions): Promise<void>;
    audit(urls: string | string[], options?: WebHintPluginOptions): Promise<AnalyzerResult[]>;
}
export declare const webHintPlugin: WebHintPlugin;
