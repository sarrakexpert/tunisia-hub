import { Configuration } from "hint";
import { InstallList } from "../../../test-api/installList";
import { DeepPartial } from "../../../types/deep-partial";
import { Engine } from "../../../types/engine";
import { TestMakerPlugin } from "../../../types/plugins/testMakerPlugin";
export interface ODiffPluginOptions extends DeepPartial<Configuration> {
    outputDiffMask: boolean;
    failOnLayoutDiff: boolean;
    threshold: number;
    antialiasing?: boolean;
}
export declare class ODiffPlugin implements TestMakerPlugin {
    name: string;
    version: string;
    description: string;
    options: ODiffPluginOptions;
    install(): Promise<InstallList>;
    init(_engine: Engine, options?: ODiffPluginOptions): Promise<void>;
    diff({ baseImagePath, compareImagePath, diffResultPath, options }: {
        baseImagePath: string;
        compareImagePath: string;
        diffResultPath: string;
        options?: ODiffPluginOptions;
    }): Promise<{
        match: true;
    } | {
        match: false;
        reason: "layout-diff" | "pixel-diff";
    }>;
}
export declare const oDiffPlugin: ODiffPlugin;
