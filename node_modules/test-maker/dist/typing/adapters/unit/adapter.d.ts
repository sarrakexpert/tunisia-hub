import { TestMakerAdapter, AdapterType } from "../../types/adapter/testMakerAdapter";
import { AdapterApi } from "../../types/adapter/adapter-api";
import { TestMakerAdapterOptions } from "../../types/adapter/test-maker-adapter-options";
import { Engine } from "../../types/engine";
export declare class UnitAdapter implements TestMakerAdapter {
    isManagedRunner: boolean;
    supportedClients: string[];
    name: string;
    type: AdapterType;
    private engine;
    private options;
    init(engine: Engine, options?: TestMakerAdapterOptions): Promise<void>;
    start(engine: Engine, options?: TestMakerAdapterOptions): Promise<void>;
    stop(): Promise<void>;
    clientStart(): Promise<AdapterApi>;
    clientRestart(): Promise<void>;
    clientStop(): Promise<void>;
}
export declare const unitAdapter: UnitAdapter;
