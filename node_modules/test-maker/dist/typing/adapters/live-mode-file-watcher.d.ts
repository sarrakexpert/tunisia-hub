/// <reference types="node" />
import { EventEmitter } from 'events';
import { AdapterProxy } from "./adapter-proxy";
export declare class LiveModeFileWatcher extends EventEmitter {
    private readonly watchers;
    private readonly lockedFiles;
    private modulesGraph;
    private lastChangedFiles;
    addFile(adapterProxy: AdapterProxy, file: string): void;
    constructor();
    stop(): void;
    private onChange;
    private watch;
}
