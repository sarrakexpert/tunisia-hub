import { AdapterProxy } from "./adapter-proxy";
export default class LiveModeKeyboardEventObserver {
    private lockKeyPress;
    private lockKeyPresTimeout;
    private adapterProxy;
    constructor(adapterProxy: AdapterProxy);
    remove(): void;
    listenToKeyEvents(): void;
    private setRawMode;
    private onKeyPress;
    private stop;
    private restart;
    private exit;
    private toggleWatching;
}
